# .env - LISTO PARA PROBAR
# Si tu SQL Server está en localhost con usuario 'sa'

# === SQL SERVER ===
DB_SERVER=localhost
DB_NAME=f1_garage_tec
DB_USER=f1_app_user
DB_PASSWORD=F1Garage!2025

# === EXPRESS ===
NODE_ENV=development
PORT=3001

# === CORS ===
FRONTEND_URL=http://localhost:3000

# === SESIONES ===
SESSION_SECRET=mi_clave_secreta_super_segura
SESSION_TIMEOUT=1800000

# === JWT (opcional) ===
JWT_SECRET=mi_jwt_secret_key
JWT_EXPIRY=7d

# === ARGON2 ===
ARGON2_SALT_LENGTH=16
ARGON2_MEMORY=65540
ARGON2_TIME=3
ARGON2_PARALLELISM=1